/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Træner;

import FormandAdgang.MemberAF;
import java.util.ArrayList;

public class HoldSelc {

    private Hold juinorTeam = new Hold();
    private Hold seniorTeam = new Hold();
    private Hold compJuniorTeam = new Hold();
    private Hold compSeniorTeam = new Hold();
    
    ArrayList<MemberAF>  Hold = new ArrayList();
    
    Hold.getMembers();
    

    public void addMembers(){
       
            /*(MemberAF) ){
           
           
       if ( && members.getMember().isCompetetive()) {
            compSeniorTeam.add(members);
        }
    }

    if(members.isSenior () 
        ){
        seniorTeam.add(members);
    }

    if(members.isJunior () 
        &&members.getMember().isCompetetive()){
        compJuniorTeam.add(members);
    }

    if(members.isJunior () 
        ){
        juniorTeam.add(members);
          */
    }

}
